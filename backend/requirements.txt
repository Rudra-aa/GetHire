# GetHire Backend — Python Dependencies
# ======================================
# Install: pip install -r requirements.txt
# Dev:     pip install -r requirements.txt -r requirements-dev.txt
#
# Pin exact versions for reproducibility in production.
# Run `pip-compile` (pip-tools) to regenerate from requirements.in if needed.

# ── Web Framework ──────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1

# ── Configuration ──────────────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.4

# ── Database — MongoDB ─────────────────────────────────────────────────────
motor==3.4.0                   # async MongoDB driver (wraps PyMongo)
pymongo==4.8.0                 # sync driver (used by Motor internally)

# ── Cache — Redis ──────────────────────────────────────────────────────────
redis==5.0.7                   # includes async support (redis.asyncio)

# ── HTTP Client (for Ollama calls) ─────────────────────────────────────────
httpx==0.27.0

# ── Security (prepared, not implemented in Sprint 0) ─────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── File Handling ──────────────────────────────────────────────────────────
python-multipart==0.0.9        # multipart/form-data upload support
aiofiles==23.2.1               # async file I/O