# GetHire Environment Variables (optional)
# Copy this to .env and fill in values if using MongoDB

# MongoDB (optional — leave empty for in-memory mode)
MONGODB_URI=

# CORS origin (default: all origins)
CORS_ORIGIN=http://localhost:5173

# Upload directory (default: backend/uploads)
UPLOAD_DIR=uploads
